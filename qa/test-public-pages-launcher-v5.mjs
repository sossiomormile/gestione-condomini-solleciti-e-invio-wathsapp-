import fs from 'node:fs';
const html=fs.readFileSync('index.html','utf8');
const fail=m=>{throw new Error(m)};
const STABLE="e162aacf0bab8f5a5dbd7230861c5e2544cf53fb";
if(!html.includes(`const STABLE_SHA='${STABLE}'`))fail('main non punta alla stabile V5 Karol collaudata');
const m=html.match(/const stableNames=\[(.*?)\];/s);if(!m)fail('stableNames non trovato');
const stable=m[1];
for(const name of ['android-local-folder-guard-v1.js','v1-source-scope-v2.js','conguaglio-identity-strict-fix-v5.js','update-certification-v1.js','drive-sync-v4.js','pdf-resoconto-v1.js','convocazioni-sourcefix-v22.js'])if(!stable.includes(`'${name}'`))fail('loader produzione mancante: '+name);
if(stable.includes("'drive-sync-v3.js'"))fail('la produzione pubblica non deve caricare Drive V3');
const pos=n=>stable.indexOf(`'${n}'`);
if(!(pos('android-local-folder-guard-v1.js')<pos('drive-sync-v4.js')))fail('guardia Android deve precedere Drive V4');
if(!(pos('engine-v6.js')<pos('v1-source-scope-v2.js')))fail('scope V1 deve caricarsi dopo engine-v6');
if(!(pos('v1-source-scope-v2.js')<pos('update-certification-v1.js')))fail('scope V1 deve precedere la certificazione');
if(!(pos('conguaglio-identity-strict-fix-v5.js')<pos('update-certification-v1.js')))fail('V5 deve precedere la certificazione');
if(!(pos('update-certification-v1.js')<pos('drive-sync-v4.js')))fail('certificazione deve precedere Drive V4');
if(!html.includes("?v=20260916-public-karolsub1"))fail('cache bust pubblico Karol SUB V5 assente');
console.log('PUBLIC PAGES LAUNCHER V5 KAROL SUB TEST PASSED');
