(()=>{
'use strict';
const VERSION='convocazioni-v1';
const ROOT_FOLDER_DEFAULT='17L4pTwKdrDxHHm2Sg6MWQx2cg_yUnTXm';
const CLIENT_ID='697823317404-01qj6tn6qcrklsb251cdup5f7imhaoq3.apps.googleusercontent.com';
const READ_SCOPE='https://www.googleapis.com/auth/drive.readonly';
const TOKEN_KEY='condo_convocazioni_google_token_test_v1';
const ROOT_KEY='condo_convocazioni_root_test_v1';
const DRAFT_PREFIX='condo_convocazioni_draft_test_v1_';
const FOLDER_MIME='application/vnd.google-apps.folder';
let tokenClient=null, accessToken='', tokenExpiresAt=0, condoFolders=[], selectedFolder=null, selectedFiles=[];
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const isoDate=()=>new Date().toISOString().slice(0,10);
function norm(s){return String(s||'').trim().toUpperCase().replace(/\s+/g,' ')}
function rootId(){return (localStorage.getItem(ROOT_KEY)||ROOT_FOLDER_DEFAULT).trim()}
function rememberRoot(v){const id=String(v||'').trim();if(id)localStorage.setItem(ROOT_KEY,id)}
function restoreToken(){try{const d=JSON.parse(sessionStorage.getItem(TOKEN_KEY)||'null');if(d?.token&&Number(d.expiresAt)>Date.now()+60000){accessToken=d.token;tokenExpiresAt=Number(d.expiresAt);return true}}catch(e){}return false}
function rememberToken(r){accessToken=r.access_token||'';tokenExpiresAt=Date.now()+Math.max(60,Number(r.expires_in)||3600)*1000;sessionStorage.setItem(TOKEN_KEY,JSON.stringify({token:accessToken,expiresAt:tokenExpiresAt}))}
function clearToken(){accessToken='';tokenExpiresAt=0;sessionStorage.removeItem(TOKEN_KEY)}
function hasToken(){return !!accessToken&&tokenExpiresAt>Date.now()+60000}
function loadGIS(){return new Promise((resolve,reject)=>{if(window.google?.accounts?.oauth2)return resolve();const old=document.querySelector('script[data-conv-gis="1"]');if(old){old.addEventListener('load',resolve,{once:true});old.addEventListener('error',()=>reject(new Error('Impossibile caricare accesso Google')),{once:true});return}const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.async=true;s.dataset.convGis='1';s.onload=resolve;s.onerror=()=>reject(new Error('Impossibile caricare accesso Google'));document.head.appendChild(s)})}
async function ensureToken(){if(hasToken()||restoreToken())return accessToken;await loadGIS();return new Promise((resolve,reject)=>{tokenClient=google.accounts.oauth2.initTokenClient({client_id:CLIENT_ID,scope:READ_SCOPE,include_granted_scopes:true,callback:r=>{if(r.error)return reject(new Error(r.error));rememberToken(r);resolve(accessToken)}});tokenClient.requestAccessToken({prompt:''})})}
async function driveFetch(path,opts={}){const r=await fetch('https://www.googleapis.com/drive/v3/'+path,{...opts,headers:{...(opts.headers||{}),Authorization:'Bearer '+accessToken}});if(r.status===401){clearToken();throw new Error('Sessione Google scaduta. Ricollega Drive.')}if(!r.ok)throw new Error('Google Drive: '+r.status);return r}
async function driveList(parentId){let files=[],pageToken='';do{const p=new URLSearchParams({q:`'${parentId}' in parents and trashed=false`,fields:'nextPageToken,files(id,name,mimeType,modifiedTime,parents,size)',orderBy:'name',pageSize:'1000',spaces:'drive'});if(pageToken)p.set('pageToken',pageToken);const d=await (await driveFetch('files?'+p)).json();files=files.concat(d.files||[]);pageToken=d.nextPageToken||''}while(pageToken);return files}
function status(msg,bad=false){const el=$('convStatus');if(el){el.textContent=msg;el.style.color=bad?'#b42318':'#52606d'}}
function ensureStyles(){if($('convocazioniStyles'))return;const s=document.createElement('style');s.id='convocazioniStyles';s.textContent=`
#convLauncherCard{border:2px solid #285f9b;background:linear-gradient(180deg,#f7fbff,#fff)}#convOpenBtn{width:100%;font-size:17px;padding:14px}#convModuleOverlay{position:fixed;inset:0;background:#f3f5f8;z-index:99999;overflow:auto;color:#172033;font-family:system-ui,sans-serif}#convModuleOverlay .conv-head{position:sticky;top:0;z-index:2;background:#123c69;color:#fff;padding:14px 16px;display:flex;align-items:center;justify-content:space-between;gap:10px}#convModuleOverlay .conv-wrap{max-width:900px;margin:auto;padding:12px}.conv-card{background:#fff;border-radius:14px;padding:14px;margin:10px 0;box-shadow:0 2px 12px #0001}.conv-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.conv-grid .full{grid-column:1/-1}.conv-label{font-size:13px;font-weight:800;margin:0 0 5px;display:block}.conv-card input,.conv-card select,.conv-card textarea{width:100%;padding:11px;border:1px solid #cbd3dd;border-radius:9px;background:#fff;font:inherit}.conv-card textarea{min-height:100px;resize:vertical}.conv-row{display:flex;gap:8px;flex-wrap:wrap}.conv-row>*{flex:1;min-width:150px}.conv-muted{font-size:13px;color:#667085}.conv-ok{color:#067647;font-weight:800}.conv-warn{color:#b54708;font-weight:800}.conv-bad{color:#b42318;font-weight:800}.conv-files{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px;margin-top:8px}.conv-file{border:1px solid #e5e7eb;border-radius:9px;padding:9px;font-size:13px}.conv-file.ok{border-color:#6bc497;background:#f4fff9}.conv-actions{display:flex;gap:8px;flex-wrap:wrap}.conv-actions button{flex:1;min-width:160px}.conv-secondary{background:#667085!important}.conv-green{background:#14804a!important}.conv-close{background:#fff!important;color:#123c69!important;padding:8px 11px!important}.conv-pilot{display:inline-block;background:#fff1c2;color:#684d00;border-radius:999px;padding:3px 8px;font-size:12px;font-weight:800}.conv-preview{border:1px solid #d8dee7;border-radius:10px;padding:14px;background:#fff}.conv-preview h2{text-align:center;margin:0 0 4px;font-size:18px}.conv-preview ol{padding-left:24px}.conv-preview li{margin:7px 0}.conv-preview .conv-delega{margin-top:22px;border-top:1px solid #bbb;padding-top:12px}.conv-preview .conv-subj{font-weight:800;text-decoration:underline;margin:18px 0 12px}@media(max-width:650px){.conv-grid{grid-template-columns:1fr}.conv-grid .full{grid-column:auto}.conv-files{grid-template-columns:1fr}#convModuleOverlay .conv-wrap{padding:8px}.conv-card{border-radius:12px;padding:12px}}
`;document.head.appendChild(s)}
function ensureLauncher(){if($('convLauncherCard'))return;const main=document.querySelector('main');if(!main)return;ensureStyles();const card=document.createElement('div');card.id='convLauncherCard';card.className='card no-print';card.innerHTML=`<b>📨 Convocazioni assembleari <span class="conv-pilot">TEST · modulo separato</span></b><p class="muted">Crea convocazioni senza modificare il motore contabile. I modelli Drive vengono per ora solo letti e verificati.</p><button id="convOpenBtn">📨 CONVOCAZIONI</button>`;main.prepend(card);$('convOpenBtn').addEventListener('click',openModule)}
function overlayHtml(){return `<div id="convModuleOverlay"><div class="conv-head"><div><b>📨 CONVOCAZIONI</b><div style="font-size:12px;opacity:.85">TEST · modulo isolato · ${VERSION}</div></div><button id="convCloseBtn" class="conv-close">✕ Chiudi</button></div><div class="conv-wrap">
<div class="conv-card"><b>1. Archivio modelli Drive</b><p class="conv-muted">Questo modulo usa una propria cartella e non scrive nei file contabili di Condominio_APP.</p><div class="conv-grid"><div class="full"><label class="conv-label">ID cartella Convocazioni</label><input id="convRootId" value="${esc(rootId())}"></div><div class="full conv-actions"><button id="convScanBtn">🔗 COLLEGA E LEGGI CONVOCAZIONI</button></div></div><div id="convStatus" class="conv-muted" style="margin-top:8px">Non collegato.</div></div>
<div class="conv-card"><b>2. Condominio e modelli</b><div class="conv-grid" style="margin-top:10px"><div class="full"><label class="conv-label">Condominio</label><select id="convCondoSelect"><option value="">Collega Drive per leggere le cartelle...</option></select></div></div><div id="convModelFiles" class="conv-files"></div></div>
<div class="conv-card"><b>3. Dati assemblea</b><div class="conv-grid" style="margin-top:10px"><div><label class="conv-label">Tipo assemblea</label><select id="convType"><option value="ordinaria">Ordinaria</option><option value="straordinaria">Straordinaria</option></select></div><div><label class="conv-label">Data documento</label><input id="convDocDate" type="date" value="${isoDate()}"></div><div><label class="conv-label">1ª convocazione - data</label><input id="convDate1" type="date"></div><div><label class="conv-label">1ª convocazione - ora</label><input id="convTime1" type="time"></div><div class="full"><label class="conv-label">1ª convocazione - luogo</label><input id="convPlace1" placeholder="es. presso il condominio"></div><div><label class="conv-label">2ª convocazione - data</label><input id="convDate2" type="date"></div><div><label class="conv-label">2ª convocazione - ora</label><input id="convTime2" type="time"></div><div class="full"><label class="conv-label">2ª convocazione - luogo</label><input id="convPlace2" placeholder="es. presso il condominio"></div><div class="full"><label class="conv-label">Ordine del giorno - un punto per riga</label><textarea id="convOdg" placeholder="Esame ed approvazione bilancio...&#10;Conferma amministratore...&#10;Varie ed eventuali."></textarea></div><div class="full"><label class="conv-label">Allegati - uno per riga</label><textarea id="convAttachments" placeholder="Bilancio consuntivo&#10;Bilancio preventivo"></textarea></div><div class="full"><label class="conv-label">Nota aggiuntiva facoltativa</label><textarea id="convNote" placeholder="Eventuali comunicazioni da riportare prima della delega."></textarea></div></div><div class="conv-actions" style="margin-top:10px"><button id="convSaveDraftBtn" class="conv-secondary">💾 Salva bozza TEST</button><button id="convPreviewBtn" class="conv-green">👁️ ANTEPRIMA CONVOCAZIONE</button></div><div id="convFormStatus" class="conv-muted" style="margin-top:8px"></div></div>
<div class="conv-card"><b>4. Anteprima / PDF</b><p class="conv-muted">Il PDF viene generato in una finestra separata per non interferire con il PDF resoconto del gestionale.</p><div id="convPreviewBox" class="conv-preview"><div class="conv-muted">Compila i dati e premi ANTEPRIMA CONVOCAZIONE.</div></div><div class="conv-actions" style="margin-top:10px"><button id="convPdfBtn" disabled>📄 GENERA PDF / STAMPA</button></div></div>
<div class="conv-card"><b>Stato sicurezza V1</b><div class="conv-ok" style="margin-top:7px">✓ Nessuna modifica a rate, conguagli, contatti o archivio contabile.</div><div class="conv-ok">✓ Accesso Drive del modulo in sola lettura.</div><div class="conv-warn">⚠ Scrittura automatica dei modelli su Drive: non ancora attiva in questa V1, verrà abilitata solo dopo il collaudo dei modelli reali.</div></div>
</div></div>`}
function openModule(){ensureStyles();if($('convModuleOverlay'))return;document.body.insertAdjacentHTML('beforeend',overlayHtml());$('convCloseBtn').onclick=closeModule;$('convScanBtn').onclick=scanDrive;$('convCondoSelect').onchange=selectCondo;$('convSaveDraftBtn').onclick=saveDraft;$('convPreviewBtn').onclick=updatePreview;$('convPdfBtn').onclick=printPdf;document.body.style.overflow='hidden'}
function closeModule(){const el=$('convModuleOverlay');if(el)el.remove();document.body.style.overflow=''}
function fileKind(name){const n=norm(name);if(/^~\$/.test(name))return 'temp';if(n.includes('CONVOCAZIONE')&&/\.DOCX$/i.test(name))return 'convocazione';if(n.includes('FRONTASPIZIO')&&/\.XLSX?$/i.test(name))return 'frontespizio';if((n==='CONSEGNA.XLSX'||n.includes('CONSEGNA CONVOCAZIONE'))&&/\.XLSX?$/i.test(name))return 'consegna';if(n.includes('CONSEGNA VERBALE')&&/\.XLSX?$/i.test(name))return 'verbale';return 'altro'}
function renderFiles(){const box=$('convModelFiles');if(!box)return;const wanted=[['convocazione','Convocazione DOCX'],['frontespizio','Frontespizio XLSX'],['consegna','Consegna convocazione XLSX'],['verbale','Consegna verbale XLSX']];const kinds=new Map();selectedFiles.filter(f=>fileKind(f.name)!=='temp').forEach(f=>{const k=fileKind(f.name);if(!kinds.has(k))kinds.set(k,f)});box.innerHTML=wanted.map(([k,label])=>{const f=kinds.get(k);return `<div class="conv-file ${f?'ok':''}"><b>${f?'✓':'○'} ${esc(label)}</b><div class="conv-muted">${f?esc(f.name):'non rilevato'}</div></div>`}).join('')+selectedFiles.filter(f=>fileKind(f.name)==='temp').map(f=>`<div class="conv-file"><b>↷ ignorato</b><div class="conv-muted">${esc(f.name)}</div></div>`).join('')}
async function scanDrive(){try{const rid=$('convRootId').value.trim();if(!rid)throw new Error('Inserisci l’ID della cartella Convocazioni.');rememberRoot(rid);status('Collegamento a Google Drive…');await ensureToken();status('Lettura cartelle Convocazioni…');const all=await driveList(rid);condoFolders=all.filter(f=>f.mimeType===FOLDER_MIME).sort((a,b)=>a.name.localeCompare(b.name,'it'));if(!condoFolders.length)throw new Error('La cartella indicata non contiene sottocartelle visibili. Verifica l’ID Drive.');const sel=$('convCondoSelect');sel.innerHTML='<option value="">Seleziona un condominio…</option>'+condoFolders.map(f=>`<option value="${esc(f.id)}">${esc(f.name)}</option>`).join('');status(`Collegato · ${condoFolders.length} cartelle condominio rilevate. Seleziona un condominio.`)}catch(e){status(e.message,true)}}
async function selectCondo(){const id=$('convCondoSelect').value;selectedFolder=condoFolders.find(f=>f.id===id)||null;selectedFiles=[];$('convModelFiles').innerHTML='';if(!selectedFolder)return;try{status('Lettura modelli di '+selectedFolder.name+'…');selectedFiles=(await driveList(selectedFolder.id)).filter(f=>f.mimeType!==FOLDER_MIME);renderFiles();loadDraft();status(`Aperto ${selectedFolder.name} · ${selectedFiles.filter(f=>fileKind(f.name)!=='temp').length} file modello rilevati.`)}catch(e){status(e.message,true)}}
function draftKey(){return DRAFT_PREFIX+(selectedFolder?.id||'nessuno')}
function formData(){return{condoName:selectedFolder?.name||$('convCondoSelect')?.selectedOptions?.[0]?.textContent||'',condoId:selectedFolder?.id||'',type:$('convType')?.value||'ordinaria',docDate:$('convDocDate')?.value||'',date1:$('convDate1')?.value||'',time1:$('convTime1')?.value||'',place1:$('convPlace1')?.value.trim()||'',date2:$('convDate2')?.value||'',time2:$('convTime2')?.value||'',place2:$('convPlace2')?.value.trim()||'',odg:($('convOdg')?.value||'').split(/\n+/).map(x=>x.trim()).filter(Boolean),attachments:($('convAttachments')?.value||'').split(/\n+/).map(x=>x.trim()).filter(Boolean),note:$('convNote')?.value.trim()||''}}
function setForm(d){if(!d)return;const map={convType:d.type,convDocDate:d.docDate,convDate1:d.date1,convTime1:d.time1,convPlace1:d.place1,convDate2:d.date2,convTime2:d.time2,convPlace2:d.place2,convOdg:(d.odg||[]).join('\n'),convAttachments:(d.attachments||[]).join('\n'),convNote:d.note};Object.entries(map).forEach(([id,v])=>{const el=$(id);if(el&&v!=null)el.value=v})}
function saveDraft(){if(!selectedFolder){$('convFormStatus').textContent='Seleziona prima un condominio.';return}const d=formData();localStorage.setItem(draftKey(),JSON.stringify(d));$('convFormStatus').textContent='Bozza TEST salvata solo su questo dispositivo.'}
function loadDraft(){try{const d=JSON.parse(localStorage.getItem(draftKey())||'null');if(d){setForm(d);$('convFormStatus').textContent='Bozza TEST ripristinata.'}else $('convFormStatus').textContent=''}catch(e){}}
function validate(d){const miss=[];if(!d.condoName)miss.push('condominio');if(!d.date1)miss.push('data 1ª convocazione');if(!d.time1)miss.push('ora 1ª convocazione');if(!d.place1)miss.push('luogo 1ª convocazione');if(!d.date2)miss.push('data 2ª convocazione');if(!d.time2)miss.push('ora 2ª convocazione');if(!d.place2)miss.push('luogo 2ª convocazione');if(!d.odg.length)miss.push('ordine del giorno');return{ok:!miss.length,missing:miss}}
function fmtDate(v){if(!v)return'';const [y,m,d]=v.split('-');return y&&m&&d?`${d}/${m}/${y}`:v}
function renderPreviewHtml(d){const v=validate(d);const title=`convocazione assemblea ${esc(d.type||'ordinaria')} condominio “${esc(d.condoName||'')}”`;const intro=`I signori condomini sono convocati in Assemblea ${esc(d.type||'ordinaria')} in prima convocazione il giorno <b>${fmtDate(d.date1)}</b> alle ore <b>${esc(d.time1)}</b> presso <b>${esc(d.place1)}</b> e, qualora mancasse il numero legale, in seconda convocazione il giorno <b>${fmtDate(d.date2)}</b> alle ore <b>${esc(d.time2)}</b> presso <b>${esc(d.place2)}</b> per discutere e deliberare sul seguente ordine del giorno:`;return `<div class="conv-doc"><h2>${esc((d.condoName||'').toUpperCase())}</h2><div class="conv-subj">Oggetto: ${title}</div><p>${intro}</p><h3 style="text-align:center">Ordine del giorno</h3><ol>${(d.odg||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ol>${d.note?`<p><b>Nota:</b> ${esc(d.note)}</p>`:''}${(d.attachments||[]).length?`<p><b>Si allega alla presente:</b></p><ul>${d.attachments.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:''}<div class="conv-delega"><h3>DELEGA</h3><p>Il sottoscritto ............................................................ in qualità di proprietario/a delega il/la sig./sig.ra ............................................................ a partecipare in sua vece all’assemblea condominiale. La delega si intende valida per entrambe le convocazioni.</p><p style="text-align:right;margin-top:28px"><b>FIRMA ______________________________</b></p></div>${v.ok?'':`<p class="conv-bad">Dati mancanti: ${esc(v.missing.join(', '))}</p>`}</div>`}
function updatePreview(){const d=formData(),v=validate(d);$('convPreviewBox').innerHTML=renderPreviewHtml(d);$('convPdfBtn').disabled=!v.ok;$('convFormStatus').innerHTML=v.ok?'<span class="conv-ok">Dati minimi completi. Anteprima pronta.</span>':'<span class="conv-bad">Mancano: '+esc(v.missing.join(', '))+'</span>';if(selectedFolder)localStorage.setItem(draftKey(),JSON.stringify(d))}
function printablePage(d){return `<!doctype html><html lang="it"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Convocazione - ${esc(d.condoName)}</title><style>@page{size:A4;margin:18mm 18mm}body{font-family:Arial,sans-serif;color:#111;font-size:12.5pt;line-height:1.45;margin:0}h2{text-align:center;font-size:16pt;margin:0 0 16px}.conv-subj{font-weight:700;text-decoration:underline;margin:12px 0}h3{text-align:center;font-size:13pt;margin:16px 0 8px}li{margin:6px 0}.conv-delega{margin-top:22px;border-top:1px solid #999;padding-top:12px}p{margin:9px 0}ul,ol{padding-left:24px}</style></head><body>${renderPreviewHtml(d)}</body></html>`}
function printPdf(){const d=formData(),v=validate(d);if(!v.ok)return alert('Completa prima: '+v.missing.join(', '));const w=window.open('','_blank');if(!w)return alert('Il browser ha bloccato la finestra PDF. Consenti i popup per questa pagina.');w.document.open();w.document.write(printablePage(d));w.document.close();w.focus();setTimeout(()=>w.print(),250)}
window.condoConvocazioniV1={version:VERSION,rootId,validate,renderPreviewHtml,printablePage,open:openModule,scanDrive};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ensureLauncher,{once:true});else ensureLauncher();
})();
